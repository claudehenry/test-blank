/**
 * @description Takes a single argument `x` and returns it unmodified.
 * 
 * @param {number} x - 💤 quantity to be returned by the function.
 * 
 * @returns {number} `x`.
 */
const f = (x) => {
  // test
  return x;
}
